#ifndef EXAMINATION_H
#define EXAMINATION_H

#include "strunct.h"

int exam_for_stack(char*, struct stack*);
int exam_for_mass(char*, char*);
int exam_for_stack_memory(char*, struct stack*, void *k[N], int*);

#endif